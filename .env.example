# Copy this file to ".env" (or export the variable in your shell) to enable AI features.
# Without a key the editor still works fully and AI generation falls back to a
# built-in procedural designer.

ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the port (defaults to 3000)
# PORT=3000
