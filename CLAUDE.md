# CLAUDE.md

This file provides guidance to AI assistants (e.g. Claude Code) when working in
this repository.

## Current state

> **This repository is currently empty** — it was freshly initialized and has no
> source code, build tooling, or commit history yet (as of 2026-06-21). This
> document is a starter scaffold. As the codebase grows, replace the placeholder
> sections below with concrete, verified details.

When real code lands, this file should be updated to describe what an assistant
actually needs to be productive here. Keep it accurate: document only what exists
and has been verified, not aspirations.

## What to fill in next

Once the project has code, expand the sections below with specifics:

### Project overview
- What this project does and who it is for.
- The primary language(s), framework(s), and runtime versions.

### Repository structure
- Top-level directory layout and the purpose of each major directory.
- Where application code, tests, configuration, and scripts live.

### Development workflow
- How to install dependencies (e.g. the package manager and lockfile in use).
- How to build the project.
- How to run the application locally.
- How to run the test suite and a single test.
- How to run the linter/formatter and type checks.

### Conventions
- Code style and formatting rules (and the tools that enforce them).
- Naming conventions and project-specific patterns to follow.
- Commit message / branch naming conventions.
- Anything non-obvious an assistant should know before making changes.

## Git workflow

- Develop on a feature branch; do not commit directly to the default branch
  unless explicitly asked.
- Write clear, descriptive commit messages.
- Do not create pull requests unless explicitly requested.
